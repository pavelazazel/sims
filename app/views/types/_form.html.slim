= form_with model: @type, local: true do |form|
  - if @type.errors.any?
      div id="error_explanation"
        h2.text-danger #{pluralize(@type.errors.count, "error")} prohibited this type from being saved:
        - @type.errors.full_messages.each do |msg|
          li.text-danger
            = msg

  / TODO: fix: when errors appear, the structure below collapses
  .form-group.form-control-sm
    p
      .form-row
        = form.label :title, class: 'ml-2 text-light col-1'
        = form.text_field :title, class: 'bg-secondary text-light custom-input'
    p
      = form.submit class: "btn btn-outline-success"
