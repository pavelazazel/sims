= form_with model: @consumable_type, local: true do |form|
  - if @consumable_type.errors.any?
      div id="error_explanation"
        h2.text-danger #{pluralize(@consumable_type.errors.count, "error")} prohibited this consumable type from being saved:
        - @consumable_type.errors.full_messages.each do |msg|
          li.text-danger
            = msg

  / TODO: fix: when errors appear, the structure below collapses
  .form-group.form-control-sm
    p
      .form-row
        = form.label :title, class: 'ml-2 text-light col-1'
        = form.text_field :title, class: 'bg-secondary text-light custom-input'
    p
      = form.submit class: "btn btn-outline-success"
